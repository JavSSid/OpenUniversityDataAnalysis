config {
  type: "incremental",
  schema: "dv",
  tags: ["dv", "hub"],
  uniqueKey: ["hk_assessment"],
  assertions: {
    nonNull: ["hk_assessment", "id_assessment"],
  },
}

SELECT DISTINCT
  TO_HEX(SHA256(CAST(id_assessment AS STRING))) AS hk_assessment,
  id_assessment,
  CURRENT_TIMESTAMP() AS dv_load_date,
  'OULAD' AS dv_source
FROM ${ref("bronze_assessments")}
